
use * from tests.sub.util

fn a_wrapper(v *vec[]int) int {
  return v.len
}

type X struct {
  a f32,

  type int
}

fn main() int
{
  printf("%d %d %lu %lu ok\n", 1, 2, offsetof(X.type), offsetof(X.a))
  return 0
}
