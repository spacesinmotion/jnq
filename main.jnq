
use tests.src.State

use tests.sub.util

fn main() int
{
  printf("main\n")

  ASSERT(9.0 == 4.0 + 5.0)
  ASSERT(util.round(-4.23) as float == -4.0f)

  // ASSERT(util.round(-4.23) as float == -4.0f)

  a int = 3
  b int = 3
  a = 10 + a
  ++b

  printf("ok\n")
}