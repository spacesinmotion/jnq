
use tests.src.Statement

use tests.sub.util

fn main() int
{
  printf("main\n")

  ASSERT(9.0 == 4.0 + 5.0)
  ASSERT(util.round(-4.23) as float == -4.0f)

  // ASSERT(util.round(-4.23) as float == -4.0f)

  n util.Nix = util.Nix.Nicest 

  printf("ok\n")
}