
fn dummy(a int) int {
  return a
}

fn main() int
{
  v := [3, 1, 2]
  printf("%d %d %d\n", v[0], v[1], v[2])

  switch (val := dummy(3)) {
  case 3: printf("Yes %d\n", val)
  default: ASSERT(false)
  }

  printf("ok\n")
  return 0
}
