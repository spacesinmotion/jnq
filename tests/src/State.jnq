
// store the location in a file and run through
type State struct {
  file *char
  c *char
  line int
  column int
}

fn new(c *char, file *char) State { 
  return State{
    file: file, 
    c: c,
    line: 1,
    column: 1 
  }
}

fn skip(s *State, c int) {
  s.c = &s.c[c]
  s.column += c
}
